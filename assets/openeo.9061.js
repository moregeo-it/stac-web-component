(self["webpackChunkvue_lib_openeo"] = self["webpackChunkvue_lib_openeo"] || []).push([[9061],{

/***/ 204:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _node_modules_vue_style_loader_index_js_clonedRuleSet_22_use_0_node_modules_css_loader_dist_cjs_js_clonedRuleSet_22_use_1_node_modules_vue_vue_loader_v15_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_2_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_3_node_modules_sass_loader_dist_cjs_js_clonedRuleSet_22_use_4_node_modules_vue_vue_loader_v15_lib_index_js_vue_loader_options_LoadingIcon_vue_vue_type_style_index_0_id_38e33be8_prod_lang_scss_scoped_true__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(6101);
/* harmony import */ var _node_modules_vue_style_loader_index_js_clonedRuleSet_22_use_0_node_modules_css_loader_dist_cjs_js_clonedRuleSet_22_use_1_node_modules_vue_vue_loader_v15_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_2_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_3_node_modules_sass_loader_dist_cjs_js_clonedRuleSet_22_use_4_node_modules_vue_vue_loader_v15_lib_index_js_vue_loader_options_LoadingIcon_vue_vue_type_style_index_0_id_38e33be8_prod_lang_scss_scoped_true__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_vue_style_loader_index_js_clonedRuleSet_22_use_0_node_modules_css_loader_dist_cjs_js_clonedRuleSet_22_use_1_node_modules_vue_vue_loader_v15_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_2_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_3_node_modules_sass_loader_dist_cjs_js_clonedRuleSet_22_use_4_node_modules_vue_vue_loader_v15_lib_index_js_vue_loader_options_LoadingIcon_vue_vue_type_style_index_0_id_38e33be8_prod_lang_scss_scoped_true__WEBPACK_IMPORTED_MODULE_0__);
/* harmony reexport (unknown) */ var __WEBPACK_REEXPORT_OBJECT__ = {};
/* harmony reexport (unknown) */ for(const __WEBPACK_IMPORT_KEY__ in _node_modules_vue_style_loader_index_js_clonedRuleSet_22_use_0_node_modules_css_loader_dist_cjs_js_clonedRuleSet_22_use_1_node_modules_vue_vue_loader_v15_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_2_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_3_node_modules_sass_loader_dist_cjs_js_clonedRuleSet_22_use_4_node_modules_vue_vue_loader_v15_lib_index_js_vue_loader_options_LoadingIcon_vue_vue_type_style_index_0_id_38e33be8_prod_lang_scss_scoped_true__WEBPACK_IMPORTED_MODULE_0__) if(__WEBPACK_IMPORT_KEY__ !== "default") __WEBPACK_REEXPORT_OBJECT__[__WEBPACK_IMPORT_KEY__] = () => _node_modules_vue_style_loader_index_js_clonedRuleSet_22_use_0_node_modules_css_loader_dist_cjs_js_clonedRuleSet_22_use_1_node_modules_vue_vue_loader_v15_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_2_node_modules_postcss_loader_dist_cjs_js_clonedRuleSet_22_use_3_node_modules_sass_loader_dist_cjs_js_clonedRuleSet_22_use_4_node_modules_vue_vue_loader_v15_lib_index_js_vue_loader_options_LoadingIcon_vue_vue_type_style_index_0_id_38e33be8_prod_lang_scss_scoped_true__WEBPACK_IMPORTED_MODULE_0__[__WEBPACK_IMPORT_KEY__]
/* harmony reexport (unknown) */ __webpack_require__.d(__webpack_exports__, __WEBPACK_REEXPORT_OBJECT__);


/***/ }),

/***/ 327:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1601);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(6314);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.loading-icon[data-v-38e33be8]{display:inline-block;width:1em;height:1em;font-size:1em;line-height:1em}.loading-icon.rotate[data-v-38e33be8]{animation:loading-icon-rotate-38e33be8 1s linear infinite}@keyframes loading-icon-rotate-38e33be8{0%{transform:rotate(0deg)}to{transform:rotate(359deg)}}`, ""]);
// Exports
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 6101:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__(327);
if(content.__esModule) content = content.default;
if(typeof content === 'string') content = [[module.id, content, '']];
if(content.locals) module.exports = content.locals;
// add CSS to Shadow Root
var add = (__webpack_require__(9547)/* ["default"] */ .A)
module.exports.__inject__ = function (shadowRoot) {
  add("0ad12776", content, shadowRoot)
};

/***/ }),

/***/ 6440:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  A: () => (/* binding */ Loading)
});

;// ./node_modules/thread-loader/dist/cjs.js!./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[1]!./node_modules/@vue/vue-loader-v15/lib/loaders/templateLoader.js??ruleSet[1].rules[3]!./node_modules/@vue/vue-loader-v15/lib/index.js??vue-loader-options!./components/internal/Loading.vue?vue&type=template&id=12eccdbd
var render = function render() {
  var _vm = this,
    _c = _vm._self._c;
  return _c('div', {
    staticClass: "vue-component loading-notice"
  }, [_c('span', {
    staticClass: "loading"
  }, [_c('LoadingIcon', {
    attrs: {
      "rotate": ""
    }
  }), _vm._v(" Loading...")], 1)]);
};
var staticRenderFns = [];

// EXTERNAL MODULE: ./components/internal/LoadingIcon.vue + 3 modules
var LoadingIcon = __webpack_require__(6781);
;// ./node_modules/thread-loader/dist/cjs.js!./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[1]!./node_modules/@vue/vue-loader-v15/lib/index.js??vue-loader-options!./components/internal/Loading.vue?vue&type=script&lang=js

/* harmony default export */ const Loadingvue_type_script_lang_js = ({
  components: {
    LoadingIcon: LoadingIcon/* default */.A
  },
  name: 'Loading'
});
;// ./components/internal/Loading.vue?vue&type=script&lang=js
 /* harmony default export */ const internal_Loadingvue_type_script_lang_js = (Loadingvue_type_script_lang_js); 
// EXTERNAL MODULE: ./node_modules/@vue/vue-loader-v15/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(1656);
;// ./components/internal/Loading.vue





/* normalize component */
;
var component = (0,componentNormalizer/* default */.A)(
  internal_Loadingvue_type_script_lang_js,
  render,
  staticRenderFns,
  false,
  null,
  null,
  null
  ,true
)

/* harmony default export */ const Loading = (component.exports);

/***/ }),

/***/ 6781:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  A: () => (/* binding */ LoadingIcon)
});

;// ./node_modules/thread-loader/dist/cjs.js!./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[1]!./node_modules/@vue/vue-loader-v15/lib/loaders/templateLoader.js??ruleSet[1].rules[3]!./node_modules/@vue/vue-loader-v15/lib/index.js??vue-loader-options!./components/internal/LoadingIcon.vue?vue&type=template&id=38e33be8&scoped=true
var render = function render() {
  var _vm = this,
    _c = _vm._self._c;
  return _c('svg', {
    staticClass: "loading-icon",
    class: {
      rotate: _vm.rotate
    },
    attrs: {
      "xmlns": "http://www.w3.org/2000/svg",
      "xmlns:xlink": "http://www.w3.org/1999/xlink",
      "xml:space": "preserve",
      "version": "1.1",
      "x": "0px",
      "y": "0px",
      "viewBox": "0 0 100 100",
      "width": "400px",
      "height": "400px"
    }
  }, [_c('path', {
    attrs: {
      "d": "M78.271,21.729C71.028,14.486,61.028,10,50,10C27.944,10,10,27.944,10,50S27.944,90,50,90S90,72.056,90,50L80,50C80,66.542,66.542,80,50,80S20,66.542,20,50S33.458,20,50,20C58.271,20,65.771,23.365,71.203,28.797L60,40L90,40L90,10L78.271,21.729Z",
      "stroke": "none"
    }
  })]);
};
var staticRenderFns = [];

;// ./node_modules/thread-loader/dist/cjs.js!./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[1]!./node_modules/@vue/vue-loader-v15/lib/index.js??vue-loader-options!./components/internal/LoadingIcon.vue?vue&type=script&lang=js
/* harmony default export */ const LoadingIconvue_type_script_lang_js = ({
  name: "LoadingIcon",
  props: {
    rotate: {
      type: Boolean,
      default: false
    }
  }
});
;// ./components/internal/LoadingIcon.vue?vue&type=script&lang=js
 /* harmony default export */ const internal_LoadingIconvue_type_script_lang_js = (LoadingIconvue_type_script_lang_js); 
// EXTERNAL MODULE: ./node_modules/@vue/vue-loader-v15/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(1656);
;// ./components/internal/LoadingIcon.vue



function injectStyles (context) {
  
  var style0 = __webpack_require__(204)
if (style0.__inject__) style0.__inject__(context)

}

/* normalize component */

var component = (0,componentNormalizer/* default */.A)(
  internal_LoadingIconvue_type_script_lang_js,
  render,
  staticRenderFns,
  false,
  injectStyles,
  "38e33be8",
  null
  ,true
)

/* harmony default export */ const LoadingIcon = (component.exports);

/***/ }),

/***/ 9061:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  A: () => (/* binding */ utils)
});

// EXTERNAL MODULE: ./node_modules/@openeo/js-commons/src/main.js
var main = __webpack_require__(1572);
// EXTERNAL MODULE: ./components/internal/Loading.vue + 3 modules
var Loading = __webpack_require__(6440);
;// ./node_modules/thread-loader/dist/cjs.js!./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[1]!./node_modules/@vue/vue-loader-v15/lib/loaders/templateLoader.js??ruleSet[1].rules[3]!./node_modules/@vue/vue-loader-v15/lib/index.js??vue-loader-options!./components/internal/Errored.vue?vue&type=template&id=d2682b08
var render = function render() {
  var _vm = this,
    _c = _vm._self._c;
  return _c('p', {
    staticClass: "vue-component component-error-notice"
  }, [_vm._v(" Sorry, could not load user interface from server. Please try again. ")]);
};
var staticRenderFns = [];

;// ./node_modules/thread-loader/dist/cjs.js!./node_modules/babel-loader/lib/index.js??clonedRuleSet-40.use[1]!./node_modules/@vue/vue-loader-v15/lib/index.js??vue-loader-options!./components/internal/Errored.vue?vue&type=script&lang=js
/* harmony default export */ const Erroredvue_type_script_lang_js = ({
  name: 'Errored'
});
;// ./components/internal/Errored.vue?vue&type=script&lang=js
 /* harmony default export */ const internal_Erroredvue_type_script_lang_js = (Erroredvue_type_script_lang_js); 
// EXTERNAL MODULE: ./node_modules/@vue/vue-loader-v15/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(1656);
;// ./components/internal/Errored.vue





/* normalize component */
;
var component = (0,componentNormalizer/* default */.A)(
  internal_Erroredvue_type_script_lang_js,
  render,
  staticRenderFns,
  false,
  null,
  null,
  null
  ,true
)

/* harmony default export */ const Errored = (component.exports);
;// ./utils.js



const SEARCH_SPLITCHARS = /[\s\.,;:"'!&\(\{\[\)\}\]]+/g;
const SEARCH_SERIALIZER = (_, value) => {
  if (value === null) {
    return "";
  } else if (typeof value === 'string') {
    return value.replace(/\s+/g, ' ');
  }
  return value;
};
class Utils extends main.Utils {
  static kebabToCamelCase(str) {
    return str.replace(/-(\w)/g, (_, c) => c ? c.toUpperCase() : '');
  }
  static enableHtmlProps(vm) {
    // Don't execute if not in web-component mode (i.e. check for the shadow root)
    if (!Utils.isObject(vm.$root) || !vm.$root.$options.shadowRoot) {
      return;
    }

    // Read the HTML props once the page is completely loaded and all props are completely available
    if (document.readyState === 'complete') {
      Utils.readHtmlProps(vm);
    } else {
      document.addEventListener('readystatechange', () => Utils.enableHtmlProps(vm), {
        once: true
      });
    }
  }
  static readHtmlProps(vm) {
    if (!Utils.isObject(vm) || !Utils.isObject(vm.$slots) || !Array.isArray(vm.$slots.default)) {
      return;
    }

    // Read script tags
    let slots = vm.$slots.default.filter(slot => typeof slot.tag === 'string' && slot.tag.toUpperCase() === 'SCRIPT' && typeof slot.data.attrs.type === 'string' && slot.data.attrs.type.includes('application/json'));
    // We are using `includes` here as for some strange reasons in Jupyter Notebooks subsequent re-renders of the cell
    // result in the attribute value being prefixed by a "true/", i.e.the value in slot.data.attrs.type is "true/application/json".
    for (let slot of slots) {
      let prop = null;
      try {
        if (typeof slot.data.attrs.prop === 'string' && slot.data.attrs.prop.length > 0) {
          prop = Utils.kebabToCamelCase(slot.data.attrs.prop);
        }
        let value = JSON.parse(slot.data.domProps.innerHTML);
        if (prop) {
          Utils.setProp(vm, prop, value); // Set a single prop
        } else if (Utils.isObject(value)) {
          for (let key in value) {
            // Set all props
            Utils.setProp(vm, key, value[key]);
          }
        } else {
          console.error(`Props passed via script tag must be contained in an object.`);
        }
      } catch (error) {
        if (prop) {
          console.error(`Prop '${prop}' passed via script tag is invalid: ${error.message}`);
        } else {
          console.error(`Props passed via script tag are invalid: ${error.message}`);
        }
      }
    }
  }
  static loadFontAwesome(vm) {
    let webComponent = Utils.isObject(vm.$root) && vm.$root.$options.shadowRoot;
    let stylesheets = Array.from((webComponent || document).styleSheets);
    for (let css of stylesheets) {
      if (typeof css.href === 'string' && css.href.includes('fontawesome')) {
        return;
      }
    }

    // Don't execute if not in web-component mode (browsers don't support loading fonts in shadowroot)
    if (!webComponent) {
      let font = document.createElement('link');
      font.as = "font";
      font.type = "font/woff2";
      font.crossOrigin = true;
      font.href = "https://use.fontawesome.com/releases/v5.13.0/webfonts/fa-solid-900.woff2";
      document.head.appendChild(font);
    }
    let css = document.createElement('link');
    css.rel = "stylesheet";
    css.type = "text/css";
    css.media = "all";
    css.href = "https://use.fontawesome.com/releases/v5.13.0/css/all.css";
    (webComponent || document.head).appendChild(css);
  }
  static setProp(vm, prop, value) {
    // Depending on when during the page load this is executed, we
    // need either to populate propsData (initially available) or
    // $props (available after propsData has been read).
    let propsRef = Utils.isObject(vm.$props) ? vm.$props : vm.$options.propsData;
    vm.$set(propsRef, Utils.kebabToCamelCase(prop), value);
  }
  static loadAsyncComponent(importer) {
    return {
      component: importer,
      loading: Loading/* default */.A,
      error: Errored,
      delay: 0,
      timeout: 10000
    };
  }
  static dataType(schema, signature = false, similarAllowed = 2, level = 0) {
    let schemaObj = new main.ProcessSchema(schema);
    var types = new Set();
    for (let i in schemaObj.schemas) {
      let dt = schemaObj.schemas[i];
      let native = dt.nativeDataType();
      let type = dt.dataType();
      // Make the data types that we can submit via JSON (e.g. raster-cube, labeled-array) native
      if (!dt.isEditable()) {
        native = type;
      } else if (signature && schemaObj.schemas.filter(other => other.nativeDataType() === native).length > similarAllowed) {
        // For signatures only: Check whether another similar type is available, then show only native type
        types.add(native);
        continue;
      }
      let formatted = native === type ? type : `${type}:${native}`;
      if (native === 'array' && Utils.isObject(dt.schema.items)) {
        let arrayItems = Utils.dataType(dt.schema.items, signature, similarAllowed, level + 1);
        if (arrayItems !== 'any') {
          formatted += `<${arrayItems}>`;
        }
      }
      types.add(formatted);
    }
    if (types.has('any')) {
      return 'any';
    }
    return Array.from(types).join(signature || level > 0 ? '|' : ', ');
  }
  static htmlentities_decode(str) {
    if (typeof str !== 'string') {
      str = String(str);
    }
    return str.replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&apos;/g, "'");
  }
  static htmlentities(str) {
    if (typeof str !== 'string') {
      str = String(str);
    }
    return str.replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, '&apos;');
  }
  static countObjectKeys(data) {
    var count = {};
    for (var i in data) {
      var obj = data[i];
      if (typeof obj !== 'object' || obj === null || Array.isArray(obj)) {
        return null;
      }
      for (var key in obj) {
        if (typeof count[key] === 'undefined') {
          count[key] = 1;
        } else {
          count[key]++;
        }
      }
    }
    return count;
  }
  static isTableLike(data, force = false) {
    if (typeof data !== 'object' || data === null) {
      return [];
    }
    var countedKeys = Utils.countObjectKeys(data);
    if (countedKeys !== null) {
      if (force === true) {
        return Object.keys(countedKeys);
      }
      var num = 0;
      var sum = 0;
      for (var i in countedKeys) {
        num++;
        sum += countedKeys[i];
      }
      var avg = sum / num;
      var dataSize = Array.isArray(dataSize) ? data.length : Object.keys(data).length;
      if (avg > dataSize / 2) {
        return Object.keys(countedKeys);
      }
    }
    return [];
  }
  static isUrl(value, onlyHttp = true) {
    if (!Utils.hasText(value)) {
      return false;
    }
    try {
      let url = new URL(value);
      if (onlyHttp && !url.protocol.match(/^https?:$/i)) {
        return false;
      }
      return true;
    } catch (error) {
      return false;
    }
  }
  static prettifyAbbreviation(str) {
    if (typeof str === 'string' && str.match(/[A-Z]+/) === null) {
      return str.toUpperCase();
    }
    return str;
  }
  static ensurePoint(pt, fallback = null) {
    if (typeof fallback !== 'function') {
      fallback = () => [0, 0];
    }
    if (!Array.isArray(pt)) {
      return fallback();
    }
    if (typeof pt[0] !== 'number') {
      pt[0] = fallback()[0] || 0;
    }
    if (typeof pt[1] !== 'number') {
      pt[1] = fallback()[1] || 0;
    }
    return pt;
  }
  static formatRef(value) {
    if (this.isRef(value)) {
      if (value.from_node) {
        return "#" + value.from_node;
      } else if (value.from_parameter) {
        return "$" + value.from_parameter;
      }
    }
    return value;
  }
  static isRef(obj) {
    return Utils.isObject(obj) && (obj.from_parameter || obj.from_node);
  }
  static isRefEqual(ref1, ref2) {
    if (!Utils.isRef(ref1) || !Utils.isRef(ref2)) {
      return false;
    } else if (ref1.from_parameter && ref1.from_parameter === ref2.from_parameter) {
      return true;
    } else if (ref1.from_node && ref1.from_node === ref2.from_node) {
      return true;
    }
    return false;
  }

  // A very rough GeoJSON detection, if no GeoJSON schema is available.
  static detectGeoJson(data) {
    if (!Utils.isObject(data)) {
      return false;
    } else if (typeof data.type !== 'string') {
      return false;
    }
    switch (data.type) {
      case "Point":
      case "MultiPoint":
      case "LineString":
      case "MultiLineString":
      case "Polygon":
      case "MultiPolygon":
        if (!Array.isArray(data.coordinates)) {
          return false;
        }
        return true;
      case "GeometryCollection":
        if (!Array.isArray(data.geometries)) {
          return false;
        }
        return true;
      case "Feature":
        if (data.geometry !== null && !Utils.isObject(data.geometry)) {
          return false;
        }
        if (data.properties !== null && !Utils.isObject(data.properties)) {
          return false;
        }
        return true;
      case "FeatureCollection":
        if (!Array.isArray(data.features)) {
          return false;
        }
        return true;
      default:
        return false;
    }
  }
  static toProcessParameters(parameters) {
    if (Utils.isObject(parameters)) {
      let processParameters = [];
      for (let name in parameters) {
        let param = parameters[name];
        let schema = Utils.omitFromObject(param, ['description', 'required', 'default', 'federation:backends']);
        processParameters.push({
          name,
          description: param.description,
          optional: !param.required,
          default: param.default,
          'federation:backends': param['federation:backends'],
          schema
        });
      }
      return processParameters.sort((a, b) => Utils.compareStringCaseInsensitive(a.name, b.name));
    } else {
      return [];
    }
  }
  static formatCurrency(amount, currency, fallback = '') {
    if (typeof amount !== 'number') {
      return fallback;
    }
    try {
      return amount.toLocaleString(undefined, {
        style: 'currency',
        currency: currency
      });
    } catch (error) {
      let str = amount.toLocaleString(undefined, {
        maximumFractionDigits: 2
      });
      if (typeof currency === 'string') {
        str += ' ';
        str += currency;
      }
      return str.trim();
    }
  }
  static formatBudget(budget, currency, unlimited = "Unlimited") {
    if (budget === null) {
      return unlimited;
    } else {
      return Utils.formatCurrency(budget, currency);
    }
  }
  static formatTimestamp(value, fallback = 'n/a') {
    if (typeof value === 'string') {
      try {
        return new Date(value).toLocaleString(undefined, {
          timeZone: "UTC",
          timeZoneName: "short"
        });
      } catch (error) {}
    }
    return fallback;
  }
  static formatFileSize(value, fallback = 'n/a') {
    let units = ['B', 'kB', 'MB', 'GB', 'TB', 'PB'];
    if (typeof value !== 'number') {
      return fallback;
    }
    let i = value == 0 ? 0 : Math.floor(Math.log(value) / Math.log(1024));
    let size = value / Math.pow(1024, i);
    let sizeStr = size.toLocaleString(undefined, {
      maximumFractionDigits: 1
    });
    return `${sizeStr} ${units[i]}`;
  }
  static formatProcessSignature(process, html = true) {
    let params = [];
    if (Array.isArray(process.parameters)) {
      for (let i in process.parameters) {
        let p = process.parameters[i];
        let pType = Utils.dataType(p.schema, true, 1);
        let req = p.optional ? '?' : '';
        let pDefault = '';
        if (p.optional && typeof p.default !== 'undefined') {
          pDefault = JSON.stringify(p.default);
        }
        let pStr;
        if (html) {
          pStr = `<span class="param-optional">${req}</span><span class="data-type">${Utils.htmlentities(pType)}</span> <span class="param-name">${Utils.htmlentities(p.name)}</span>`;
          if (pDefault) {
            if (pDefault.length > 30) {
              pDefault = `<span title="${Utils.htmlentities(pDefault)}">…</span>`;
            }
            pStr += ` = <span class="param-argument">${pDefault}</span>`;
          }
        } else {
          pStr = req + pType + " " + p.name + pDefault;
        }
        params.push(pStr);
      }
    }
    let paramStr = "(" + params.join(", ") + ") : ";
    let returnSchema = Utils.isObject(process.returns) && process.returns.schema && typeof process.returns.schema === 'object' ? process.returns.schema : {};
    let returns = Utils.dataType(returnSchema, true, 2);
    if (html) {
      return `<span class="process-name">${Utils.htmlentities(process.process.id)}</span>${paramStr}<span class="data-type">${Utils.htmlentities(returns)}</span>`;
    } else {
      return process.process.id + paramStr + returns;
    }
  }
  static search(searchterm, target, and = true) {
    if (typeof searchterm !== 'string' || searchterm.length === 0) {
      return false;
    }
    if (Utils.isObject(target)) {
      target = Object.values(target);
    } else if (typeof target === 'string') {
      target = [target];
    }
    if (!Array.isArray(target)) {
      return false;
    }

    // Prepare search terms
    searchterm = searchterm.toLowerCase().split(SEARCH_SPLITCHARS);

    // Prepare text to search in
    target = target.map(v => {
      if (typeof v === 'string') {
        return v;
      } else {
        // Convert non-strings to a string (JSON-related chars "': are added to SEARCH_SPLITCHARS)
        return JSON.stringify(v, SEARCH_SERIALIZER, 0);
      }
    }).join(' ') // Merge into a single string
    .replace(SEARCH_SPLITCHARS, ' ') // replace split chars with white spaces
    .toLowerCase(); // Lowercase

    // Search with "and" or "or"
    let fn = and ? 'every' : 'some';
    return searchterm[fn](term => target.includes(term));
  }
}
;
/* harmony default export */ const utils = (Utils);

/***/ })

}]);
//# sourceMappingURL=openeo.9061.js.map